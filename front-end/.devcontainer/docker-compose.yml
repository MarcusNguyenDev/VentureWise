# Dev container overlay on top of ../docker-compose.yml.
#
# VS Code attaches to the container, so the service idles instead of starting
# the dev server — run it yourself from an integrated terminal.

name: venturewise

services:
  front-end:
    command: sleep infinity
